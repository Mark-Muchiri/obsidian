# File location
# /etc
## Enable soft wrapping (disables hard wrapping)
set softwrap

## Wrap at whitespace instead of character boundaries
set atblanks

## Customize wrapping further:
set fill 80           # Wrap at 80 columns (default is screen width)
set constantshow      # Always show position stats
set mouse             # Enable mouse support

## Use this spelling checker instead of the internal one.  This option
## does not have a default value.
set speller "hunspell"

## To include most of the existing syntax definitions, you can do:
include /usr/share/nano/*.nanorc

